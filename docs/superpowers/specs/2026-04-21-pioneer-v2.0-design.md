# Pioneer Game v2.0 — Design Document

**Feature**: pioneer-game-v2.0
**Phase**: Design
**Date**: 2026-04-21
**Status**: Draft
**Base Version**: pioneer-game (v1.0 97% Match) + 현재 구현 (1417줄, v2.x 기능 포함)
**Version Strategy**: Option B — 현재 구현을 v2.0 공식 베이스로 인정, 신규 시스템 추가

---

## Executive Summary

| 관점 | 내용 |
|------|------|
| **Problem** | 엔드게임 없음, 세션 설계 없음, 수익화 미구현, FTUE 부재, 선원 역할 단조로움 |
| **Solution** | 생존형 엔드게임(지수 세금) + 방치형 루프 + 보석 5종 수익화 + 이벤트/퀘스트 시스템 + 선원 역할 분화 + 인터랙티브 튜토리얼 |
| **Core Loop** | 배 출발 → 방치 → 도착 알림 → 접속 → 거래·퀘스트·이벤트 처리 → 다음 항해 출발 |
| **Core Value** | "세금에 맞서 얼마나 오래 살아남는가" — 생존이 목표, 방치가 플레이 방식 |

---

## 1. 아키텍처

### 1.1 파일 구조 (v1.x 단일 파일 → v2.0 모듈 분리)

```
src/
├── App.jsx                  # 조율자 — state 연결·라우팅만 담당
├── main.jsx
├── index.css
├── constants/
│   ├── ships.js             # SHIP_TYPES, PORT_SHIPS
│   ├── ports.js             # PORTS, REGION_STYLE, RESOURCE_REGIONS
│   ├── resources.js         # RESOURCES
│   └── crew.js              # SPECIAL_CREW_POOL, CREW_NAMES, CREW_ROLES
├── systems/
│   ├── economy.js           # 시세 계산, 세금, 거래가 공식, 밸런스 상수
│   ├── events.js            # 이벤트 테이블, 발생 로직, 회피 처리
│   ├── quests.js            # 퀘스트 풀, 생성, 판정, 보상·패널티
│   └── save.js              # localStorage 저장/불러오기 (pioneer_save_v2)
└── components/
    ├── GameMap.jsx           # 지도 + Pan/Zoom (기존 유지)
    ├── ShipPanel.jsx         # 함대 관리
    ├── TradePanel.jsx        # 무역
    ├── CrewPanel.jsx         # 선원 관리 (역할 시스템 반영)
    ├── InfoPanel.jsx         # 정보 구매
    ├── QuestPanel.jsx        # 퀘스트 목록 + 수락/포기
    ├── EventOverlay.jsx      # 이벤트 팝업 + 회피 버튼
    ├── TutorialOverlay.jsx   # FTUE 단계별 가이드
    └── GameLog.jsx           # 로그
```

### 1.2 State 구조

```js
// gs (GameState) — localStorage 저장 대상
{
  saveVersion: '2.0',
  gold: number,
  gems: number,
  ships: Ship[],
  crew: Crew[],
  availableCrew: Crew[],
  purchasedInfo: {},
  predictions: Prediction[],
  taxLevel: number,          // 세금 레벨 (1~)
  taxDueAt: number,          // 다음 세금 납부 timestamp
  quests: Quest[],           // 진행 중 퀘스트
  tutorialStep: number,      // -1 = 완료
}

// 로컬 UI state (저장 안 함)
{
  prices: {},
  priceHistory: {},
  weather: {},
  activeEvent: Event | null,
  paused: boolean,
  tab: string,
  mapView: {},
}
```

---

## 2. 핵심 게임 루프

### 2.1 방치형 루프

```
[접속]
  ↓
도착한 배 확인 → 화물 판매
  ↓
퀘스트 처리 (완료/실패 판정)
  ↓
이벤트 확인 (팝업 처리)
  ↓
다음 항구로 배 출발
  ↓
[방치 — 앱 종료 가능]
  ↓
1시간마다 시세 변동 (헤더 배지 "⚡ 시세 변동됨")
배 도착 시 로그 기록
  ↓
[재접속]
```

**방치 설계 원칙**
- 항해 중 플레이어 행동 없음 (의도적 공백)
- 도착 알림: 로그 최상단 강조 표시
- 시세 변동 배지: 헤더에 ⚡ 아이콘 표시, 클릭 시 배지 제거

### 2.2 세션 길이별 경험

| 세션 | 행동 |
|------|------|
| 1~2분 | 도착 배 화물 판매 + 다음 항구 출발 |
| 5~10분 | 위 + 퀘스트 확인 + 선원 관리 |
| 20~30분 | 위 + 정보 구매 + 배 업그레이드 + 이벤트 대응 |

---

## 3. 생존 시스템 (세금)

### 3.1 세금 공식

```js
const calcTax = (taxLevel, shipCount) => {
  const base = 50 * Math.pow(1.3, Math.max(0, taxLevel - 1));
  const shipBonus = shipCount * Math.max(20, Math.floor(20 * Math.pow(1.1, taxLevel - 1)));
  return Math.floor(base + shipBonus);
};
```

### 3.2 세금 레벨 기대값

| 레벨 | 납부액(배 3척 기준) | 느낌 |
|------|-------------------|------|
| 1 | ~110금 | 무시 가능 |
| 5 | ~366금 | 슬슬 신경 쓰임 |
| 10 | ~1,335금 | 압박 시작 |
| 15 | ~4,835금 | 긴장감 |
| 20 | ~17,490금 | 극한 경영 |

### 3.3 세금 레벨 상승 조건

- 배 1척 추가 구매 시 → +1레벨
- 누적 획득 금화 10,000금마다 → +1레벨 (축적 억제, 현재 보유량 아님)

### 3.4 납부 주기 및 미납 처리

- **납부 주기**: 게임 내 24시간 (실제 24분)마다 자동 징수
- **미납 시**: 금화 부족 → 가장 저렴한 배 1척 압류
- **배 전부 압류 시**: 게임 오버

### 3.5 세금 면제 (보석)

- 보석 2개 소비 → 다음 세금 1회 면제
- 헤더에 면제 상태 표시: 🛡️ 세금 면제 중

---

## 4. 이벤트 시스템

### 4.1 발생 구조

- **판정 시점**: 시세 갱신(1시간)마다 항해 중인 배 각각에 독립 판정
- **기본 발생 확률**: 배당 15%
- **항해사 선원 효과**: 배당 이벤트 확률 -5% (항해사 1명당)

### 4.2 이벤트 테이블

| 이벤트 | 아이콘 | 가중치 | 효과 | 회피 비용 |
|--------|--------|--------|------|-----------|
| 풍랑 | ⛈️ | 30 | 도착 시간 +50%, 각 자원 수량 5~15% 랜덤 손실 | 💎 1개 |
| 해적 출몰 | 🏴‍☠️ | 25 | 화물 20~40% 강탈 | 💎 2개 |
| 대공황 | 📉 | 15 | 다음 시세 전 항구 -30% | 💎 3개 |
| 빙하 충돌 | 🧊 | 15 | hull -50, 속도 -30% (수리 전까지) | 💎 2개 |
| 역병 창궐 | ☠️ | 10 | 목적지 항구 입항 불가 (1회 갱신) | 💎 2개 |
| 순풍 행운 | 💨 | 5 | 도착 시간 -30% (긍정, 회피 불필요) | — |

### 4.3 이벤트 UI 흐름

```
이벤트 발생
  ↓
EventOverlay 팝업 (화면 중앙):
"⛈️ [슬루프 1호]가 풍랑을 만났습니다!"
[💎 1개로 회피]  [감수하기]
  ↓
감수 → 효과 즉시 적용 + 로그 기록
회피 → 보석 차감, 이벤트 무효 + 로그 기록
```

### 4.4 선체 내구도 시스템

```js
// Ship 필드 추가
hull: number  // 0~100, 초기 100

// 빙하 충돌 시
hull -= 50;
if (hull <= 0) { /* 배 침몰 — 화물 전량 손실, ships에서 제거 */ }

// 수리 비용 (항구 도착 시)
const repairCost = (100 - ship.hull) * 50;
```

---

## 5. 퀘스트 시스템

### 5.1 퀘스트 생성

- **생성 시점**: 항구 도착 시 0~2개 랜덤 생성
- **최대 동시 진행**: 3개
- **수락 여부**: 플레이어 선택 (거절 시 소멸, 패널티 없음)

### 5.2 퀘스트 종류

| 퀘스트 | 조건 | 제한 | 성공 보상 | 실패 패널티 |
|--------|------|------|-----------|-------------|
| 긴급 배송 | 자원 10~30개 → 지정 항구 | 2시간 | 금화(세금 2배) + 💎1 | 세금 1회분 추가 |
| 항구 방문 | 3~5개 항구 탐방 | 4시간 | 금화(세금 3배) + 특수 선원 1명 | 세금 1회분 추가 |
| 무역왕 | 특정 자원으로 5,000~20,000금 수익 | 3시간 | 금화(세금 2배) + 세금 레벨 -1 | 세금 1회분 추가 |
| 위기 구조 | 폭풍 이벤트 중 배 1척 귀환 | 1시간 | 💎 2개 | 세금 1회분 추가 |
| 독점 계약 | 특정 자원 20~50개 매입 | 2시간 | 금화(세금 2배) + 해당 자원 판매가 +20% (1회) | 세금 1회분 추가 |

### 5.3 보상 설계 원칙

- 금화 보상: 현재 세금 1회분의 2~3배 (항상 수락 가치 있음)
- 보석 보상: 어려운 퀘스트에만 포함 (주요 무과금 획득 경로)
- 세금 레벨 -1: 희귀 보상, 최강 동기 부여

### 5.4 퀘스트 데이터 구조

```js
{
  id: string,
  type: 'delivery' | 'visit' | 'trade' | 'rescue' | 'monopoly',
  description: string,
  condition: {},        // 퀘스트별 달성 조건
  deadlineAt: number,  // timestamp
  reward: { gold, gems, crewId, taxLevelDelta },
  penalty: { taxMultiplier: 1 },  // 세금 1회분 추가
  status: 'active' | 'completed' | 'failed',
}
```

---

## 6. 선원 특화 시스템

### 6.1 역할 4종

| 역할 | 아이콘 | 주 효과 | 부 효과 |
|------|--------|---------|---------|
| 항해사 | ⚓ | 배 속도 +N% | 이벤트 발생률 -5% |
| 상인 | 💰 | 구매가 -N%, 판매가 +N% | — |
| 화물관리사 | 📦 | 수납량 +N칸 | 연료 소모 -5% |
| 기관사 | ⚙️ | 연료 효율 +N% | 선체 수리비 -10% |

### 6.2 등급 4단계

| 등급 | 색상 | 주 효과 범위 | 고용 방법 |
|------|------|-------------|-----------|
| 일반 | ⬜ | +5~10% | 금화 (항구) |
| 숙련 | 🟦 | +10~18% | 금화 (항구, 드묾) |
| 전문가 | 🟪 | +18~28% | 💎 1~2개 |
| 전설 | 🟧 | +28~40% | 💎 3개 + 특수 퀘스트 |

### 6.3 선원 업그레이드 (보석)

```
일반 → 숙련:    💎 1개
숙련 → 전문가:  💎 2개
전문가 → 전설:  💎 3개
```

### 6.4 중첩 공식 (수확 체감)

```js
// 같은 역할 선원 N명 효과 중첩
const stackedEffect = 1 - Math.pow(1 - individualEffect, crewCount);
```

### 6.5 선원 데이터 구조

```js
{
  id: string,
  name: string,
  role: 'navigator' | 'merchant' | 'cargo' | 'engineer',
  grade: 'common' | 'skilled' | 'expert' | 'legendary',
  primaryStat: number,    // 주 효과 수치 (소수, 예: 0.12 = 12%)
  shipId: string | null,
  hireCost: number,
}
```

---

## 7. 보석 수익화 시스템

### 7.1 획득 경로

| 방법 | 수량 | 빈도 |
|------|------|------|
| 게임 시작 지급 | 5개 | 1회 |
| 퀘스트 보상 | 1~2개 | 퀘스트당 |
| 위기 구조 퀘스트 완료 | 2개 | 희귀 |
| 튜토리얼 완료 | 2개 | 1회 |
| 인앱 결제 | 10~80개 | 구매 시 |

**무과금 기대 획득**: 주당 5~8개 (퀘스트 중심)

### 7.2 소비 5종

| 용도 | 비용 | 설명 |
|------|------|------|
| ⚡ 즉시 도착 | 1개 | 항해 중 배 즉시 목적지 도착 |
| 👤 전문가+ 선원 고용 | 1~3개 | 고급 선원 직접 고용 |
| ⬆️ 선원 업그레이드 | 1~3개 | 등급 1단계 상승 |
| 🛡️ 세금 면제 | 2개 | 다음 세금 1회 면제 |
| 🌀 이벤트 회피 | 1~3개 | 발생 이벤트 무효화 |

### 7.3 인앱 결제 패키지

| 패키지 | 보석 | 가격 | 타겟 상황 |
|--------|------|------|-----------|
| 소량팩 | 10개 | ₩1,900 | 이벤트 위기 1회 |
| 중량팩 | 35개 | ₩5,900 | 선원 육성 집중 |
| 대량팩 | 80개 | ₩11,900 | 세금 압박 탈출 |

---

## 8. FTUE / 튜토리얼

### 8.1 세계관 슬라이드 (3장)

| 장 | 내용 |
|----|------|
| 1 | "대항해시대 1492년. 당신은 빚더미 위의 신출내기 상인. 통통배 1척과 100금만 있습니다." |
| 2 | "세계 최고의 무역왕이 목표. 단, 왕국은 끝없이 세금을 요구합니다. 살아남을 수 있겠습니까?" |
| 3 | "핵심: 배를 보내고 방치하세요. 도착하면 돌아와 거래하고 다시 떠나면 됩니다." |

### 8.2 인터랙티브 튜토리얼 단계

```
tutorialStep 0: 세계관 슬라이드 표시
tutorialStep 1: [함선 탭 하이라이트] "런던에서 슬루프를 구매하세요"
                → 구매 완료 시 step 2
tutorialStep 2: [선원 탭 하이라이트] "선원 1명을 고용하세요"
                → 고용 완료 시 step 3
tutorialStep 3: [지도 하이라이트] "배를 클릭하고 목적지를 선택하세요"
                → 출발 완료 시 step 4
tutorialStep 4: [화물 탭 하이라이트] "도착 후 자원을 구매해보세요"
                → 구매 완료 시 step -1 (완료)
tutorialStep -1: 완료 보상 지급
```

### 8.3 완료 보상

```
💰 500금 지급
💎 보석 2개 지급
첫 번째 퀘스트 자동 부여 (긴급 배송 — 쉬운 난이도)
```

---

## 9. 저장/불러오기 시스템

```js
// 저장 키
const SAVE_KEY = 'pioneer_save_v2';

// saveGame()
const saveData = {
  saveVersion: '2.0',
  savedAt: new Date().toISOString(),
  gs,
  priceHistory,
};
localStorage.setItem(SAVE_KEY, JSON.stringify(saveData));

// loadGame() — 초기화 시 1회
const raw = localStorage.getItem(SAVE_KEY);
if (!raw) return null;  // 새 게임
const data = JSON.parse(raw);
if (data.saveVersion !== '2.0') return null;  // 구버전 무시
setGs(data.gs);
setPriceHistory(data.priceHistory ?? {});

// 자동 저장: 시세 갱신마다 + 항구 도착마다
```

---

## 10. 경제 밸런스

### 10.1 골드 획득 기대값 (시간당)

| 단계 | 배 구성 | 기대 수익/시간 | 세금/24분 |
|------|---------|--------------|-----------|
| 초반 | 슬루프 1척 | ~800금 | ~110금 |
| 중반 | 상인선 2척 | ~3,000금 | ~1,335금 |
| 후반 | 갤리온 3척 | ~8,000금 | ~4,835금 |
| 극후반 | 갤리온 5척 | ~14,000금 | ~17,490금 |

### 10.2 배 구매 시간 목표

| 배 | 비용 | 목표 획득 시간 |
|----|------|--------------|
| 슬루프 | 3,000금 | ~4시간 |
| 상인선 | 20,000금 | ~1일 |
| 갤리온 | 45,000금 | ~2일 |
| 프리깃 | 65,000금 | ~3일 |

### 10.3 정보 구매 ROI

| 정보 | 비용 | 평균 수익 기대값 | ROI |
|------|------|----------------|-----|
| 거리 소문 | 300금 | 적중 30% × 평균 30금 = 9금 | ❌ 손해 (정보 가치 체험용) |
| 상인 귀띔 | 700금 | 적중 40% × 평균 45금 = 18금 | ❌ 손해 (전략적 구매 필요) |
| 상업 분석 | 3,000금 | 적중 58% × 평균 95금 = 55금 | ❌ 단기 손해, 장기 전략용 |
| 내부 정보 | 8,000금 | 적중 72% × 평균 150금 = 108금 | ❌ 단기 손해, 고위험 전략 |

> **설계 의도**: 정보 구매는 단순 수익 극대화가 아니라 "불확실성 감소"에 가치가 있음.
> 대량 거래 시 정보가 있으면 수백~수천 금 차이 발생 → 전략적으로 의미 있음.

---

## 11. 성공 기준

| 기준 | 목표 |
|------|------|
| 저장/불러오기 | 저장 후 새로고침 시 금화·배·퀘스트 완벽 복원 |
| 세금 압박 | 세금 레벨 15에서 플레이어가 명확한 긴장감 느낌 |
| 이벤트 | 시세 갱신마다 항해 중 배에 15% 확률 정상 발생 |
| 퀘스트 | 항구 도착 시 퀘스트 0~2개 생성, 최대 3개 동시 진행 |
| 선원 특화 | 역할별 효과가 계산에 정확히 반영 |
| FTUE | 튜토리얼 4단계 완료 후 보상 정상 지급 |
| 보석 소비 | 5종 용도 모두 정상 작동 |
| 코드 구조 | constants/systems/components 분리, App.jsx 200줄 이내 |
| 비주얼 | CR 스타일 3D 버튼·패널, 골드 지도 항로, 등급별 선원 카드 글로우 적용 |

---

## 12. 비주얼 디자인 (Clash Royale 스타일)

### 12.1 컬러 팔레트

| 역할 | 색상 코드 | 용도 |
|------|-----------|------|
| 배경 (딥) | `#0d1b2a` | 전체 배경, 패널 기저 |
| 배경 (미드) | `#1a2f4a` | 카드·패널 본체 |
| 골드 (주) | `#f5c842` | 테두리, 강조 텍스트, 버튼 |
| 골드 (어두움) | `#c49a1a` | 버튼 하단 3D 그림자 |
| 보라 (전문가) | `#9b59b6` | 전문가 등급 선원 카드 |
| 주황 (전설) | `#e67e22` | 전설 등급 선원 카드 |
| 파랑 (숙련) | `#2980b9` | 숙련 등급 선원 카드 |
| 청록 (보석) | `#00e5ff` | 보석 아이콘, 수량 표시 |
| 위험 (빨강) | `#e74c3c` | 세금 경고, 이벤트 알림 |

### 12.2 tailwind.config.js 확장

```js
theme: {
  extend: {
    colors: {
      cr: {
        'bg-deep':   '#0d1b2a',
        'bg-mid':    '#1a2f4a',
        'gold':      '#f5c842',
        'gold-dark': '#c49a1a',
        'gold-shadow':'#6b4f0a',
        'gem':       '#00e5ff',
        'danger':    '#e74c3c',
        'rare':      '#2980b9',
        'epic':      '#9b59b6',
        'legendary': '#e67e22',
      }
    },
    boxShadow: {
      'cr-btn':   '0 4px 0 #3a2800, 0 0 12px rgba(245,200,66,0.4)',
      'cr-panel': '0 0 20px rgba(245,200,66,0.15)',
      'cr-glow':  '0 0 16px rgba(0,229,255,0.5)',
    }
  }
}
```

### 12.3 공통 CSS 클래스 (index.css)

```css
/* 3D 골드 버튼 */
.btn-cr {
  background: linear-gradient(180deg, #f5c842 0%, #e5a820 60%, #c49a1a 100%);
  border: 3px solid #8b6914;
  border-bottom: 5px solid #6b4f0a;
  border-radius: 12px;
  box-shadow: 0 4px 0 #3a2800, 0 0 12px rgba(245,200,66,0.4);
  text-shadow: 0 1px 2px rgba(0,0,0,0.6);
  font-weight: 800;
  transition: transform 0.05s, border-bottom 0.05s;
}
.btn-cr:active {
  transform: translateY(3px);
  border-bottom: 2px solid #6b4f0a;
}

/* 위험 버튼 (빨강) */
.btn-cr-danger {
  background: linear-gradient(180deg, #e74c3c 0%, #c0392b 100%);
  border: 3px solid #922b21;
  border-bottom: 5px solid #641e16;
  border-radius: 12px;
  box-shadow: 0 4px 0 #3b0f0a;
}

/* 패널 카드 */
.panel-cr {
  background: linear-gradient(180deg, #1e3a5f 0%, #152d4a 100%);
  border: 2px solid #f5c842;
  border-radius: 16px;
  box-shadow: inset 0 1px 0 rgba(255,255,255,0.1), 0 0 20px rgba(245,200,66,0.15);
}

/* 등급별 선원 카드 테두리 */
.card-common    { border: 2px solid #7f8c8d; }
.card-skilled   { border: 2px solid #2980b9; box-shadow: 0 0 8px rgba(41,128,185,0.5); }
.card-expert    { border: 2px solid #9b59b6; box-shadow: 0 0 8px rgba(155,89,182,0.6); }
.card-legendary { border: 2px solid #e67e22; box-shadow: 0 0 12px rgba(230,126,34,0.8); }

/* 골드 펄스 (선택된 항구) */
@keyframes cr-pulse {
  0%, 100% { box-shadow: 0 0 0 0 rgba(245,200,66,0.7); }
  50%       { box-shadow: 0 0 0 8px rgba(245,200,66,0); }
}
.pulse-gold { animation: cr-pulse 1.5s ease-in-out infinite; }

/* 항로 골드 점선 애니메이션 */
@keyframes dash-move {
  to { stroke-dashoffset: -20; }
}
.route-line { animation: dash-move 0.8s linear infinite; }
```

### 12.4 지도 비주얼 개편

| 요소 | 현재 | v2.0 |
|------|------|------|
| 바다 배경 | 단색 파랑 | `#0d2137` → `#0a3d5c` 방사형 그라데이션 |
| 항구 마커 | 작은 원형 점 | 지역별 아이콘 (🏰 유럽 / ⛪ 지중해 / 🕌 아라비아 / 🛕 남아시아 / 🏯 동아시아) |
| 항구 이름표 | 흰 텍스트 | 골드 테두리 다크 배지 (`panel-cr` 미니 버전) |
| 배 아이콘 | 이모지 | 이모지 + 흰 드롭 글로우 (`filter: drop-shadow`) |
| 항로 선 | 흰 점선 | 골드 점선 (`stroke: #f5c842`) + `dash-move` 애니메이션 |
| 선택된 항구 | 강조 없음 | `pulse-gold` 애니메이션 링 |
| 배 소속 표시 | 없음 | 배 아이콘 하단 골드 이름 배지 |

### 12.5 헤더 레이아웃

```
┌────────────────────────────────────────────────────────┐
│  ⚓ PIONEER   │  💰 12,450금   │  💎 8   │  ⚡ 시세변동  │
│  [크림슨 골드 로고]   [골드 텍스트]  [청록 텍스트]  [주황 배지] │
│  배경: #0d1b2a / 하단 테두리: 2px solid #f5c842        │
└────────────────────────────────────────────────────────┘
```

### 12.6 성공 기준 (비주얼)

| 기준 | 목표 |
|------|------|
| 버튼 3D 효과 | 클릭 시 눌리는 느낌 (translateY + border-bottom 감소) |
| 지도 배경 | 딥 네이비 그라데이션, 단색 제거 |
| 항구 아이콘 | 지역별 5종 이모지 구분 |
| 선원 카드 등급 | 4색 테두리 + 글로우 적용 |
| 항로 애니메이션 | 골드 점선이 흐르는 효과 |
| 전체 톤 | 다크 판타지, CR 특유의 "묵직함" 느낌 |

---

## 13. 미구현 (v2.1+ 예정)

| 기능 | 버전 |
|------|------|
| 보석 인앱 결제 실제 연동 | v2.1 |
| 경쟁 NPC 상인 | v2.2 |
| 업적 시스템 | v2.2 |
| 모바일 최적화 | v2.3 |
| 리더보드 (서버 필요) | v3.0 |
