# Pioneer v2.0 — Plan C: Integration (저장·FTUE·보석·최종 연결)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **전제조건:** Plan A + Plan B 완료

**Goal:** 저장/불러오기·FTUE 튜토리얼·보석 소비 5종을 구현하고, 전체 시스템을 App.jsx에 최종 연결해 v2.0을 완성한다.

**Architecture:** save.js로 localStorage 저장 추상화. TutorialOverlay는 tutorialStep state로 제어. 보석 소비는 기존 booster 패턴 확장. App.jsx는 최종 200줄 이내 coordinator로 정리.

**Tech Stack:** React 18, Vite 5, TailwindCSS 3

---

## 파일 맵

| 파일 | 역할 | 상태 |
|------|------|------|
| `src/systems/save.js` | saveGame, loadGame | 신규 |
| `src/components/TutorialOverlay.jsx` | FTUE 단계별 가이드 | 신규 |
| `src/components/GemShop.jsx` | 보석 소비 5종 UI | 신규 |
| `src/App.jsx` | 전체 최종 연결 + 정리 | 수정 |

---

## Task 1: save.js 작성

**Files:**
- Create: `src/systems/save.js`

- [ ] **Step 1: 파일 생성**

```js
// src/systems/save.js
const SAVE_KEY = 'pioneer_save_v2';

export const saveGame = (gs, priceHistory = {}) => {
  try {
    const data = {
      saveVersion: '2.0',
      savedAt: new Date().toISOString(),
      gs,
      priceHistory,
    };
    localStorage.setItem(SAVE_KEY, JSON.stringify(data));
    return true;
  } catch (e) {
    console.warn('Save failed:', e);
    return false;
  }
};

export const loadGame = () => {
  try {
    const raw = localStorage.getItem(SAVE_KEY);
    if (!raw) return null;
    const data = JSON.parse(raw);
    if (data.saveVersion !== '2.0') return null;
    return data;
  } catch (e) {
    return null;
  }
};

export const deleteSave = () => {
  localStorage.removeItem(SAVE_KEY);
};

export const hasSave = () => !!localStorage.getItem(SAVE_KEY);
```

- [ ] **Step 2: App.jsx에 save.js 연결**

App.jsx 상단에 import:
```js
import { saveGame, loadGame, deleteSave, hasSave } from './systems/save.js';
```

App.jsx state 초기화 수정 — 저장 데이터 감지:
```js
const [saveExists, setSaveExists] = useState(() => hasSave());
const [saveDecided, setSaveDecided] = useState(false);
const [lastSaved, setLastSaved] = useState(null);
const [priceHistory, setPriceHistory] = useState({});
```

App.jsx 저장 버튼 핸들러:
```js
const handleSave = useCallback(() => {
  const ok = saveGame(gsRef.current, priceHistory);
  if (ok) {
    setLastSaved(new Date().toLocaleTimeString());
    addLog('💾 게임이 저장되었습니다.');
  }
}, [priceHistory, addLog]);
```

App.jsx 불러오기 핸들러:
```js
const handleLoad = useCallback(() => {
  const data = loadGame();
  if (data) {
    setGs(data.gs);
    setPriceHistory(data.priceHistory ?? {});
    addLog(`💾 저장된 게임을 불러왔습니다. (${data.savedAt})`);
  }
  setSaveDecided(true);
}, [addLog]);

const handleNewGame = useCallback(() => {
  deleteSave();
  setSaveExists(false);
  setSaveDecided(true);
  addLog('🆕 새 게임을 시작합니다.');
}, []);
```

App.jsx 시세 갱신 useEffect 마지막에 자동 저장:
```js
// 시세 갱신 후 자동 저장
saveGame(gsRef.current, priceHistoryRef.current ?? {});
```

App.jsx 헤더에 저장 버튼 + 마지막 저장 시각 표시:
```jsx
<button className="btn-cr text-xs" onClick={handleSave}>
  💾 저장
</button>
{lastSaved && (
  <span className="text-xs text-gray-400">저장됨 {lastSaved}</span>
)}
```

App.jsx JSX 내 불러오기 다이얼로그 (saveExists && !saveDecided 조건):
```jsx
{saveExists && !saveDecided && (
  <div className="fixed inset-0 z-50 flex items-center justify-center"
    style={{ background: 'rgba(0,0,0,0.85)' }}>
    <div className="panel-cr p-8 text-center max-w-xs w-full mx-4 animate-fadeInUp">
      <div className="text-4xl mb-3">💾</div>
      <h2 className="font-black text-lg mb-2" style={{ color: '#f5c842' }}>
        저장된 게임 발견
      </h2>
      <p className="text-sm text-gray-300 mb-6">이어서 플레이하시겠습니까?</p>
      <div className="flex gap-3 justify-center">
        <button className="btn-cr" onClick={handleLoad}>계속하기</button>
        <button className="btn-cr-danger" onClick={handleNewGame}>새 게임</button>
      </div>
    </div>
  </div>
)}
```

- [ ] **Step 3: 빌드 + 수동 확인**

```bash
npm run build && npm run preview
```

수동 확인:
- [ ] 저장 버튼 클릭 → "게임이 저장되었습니다" 로그
- [ ] 새로고침 시 불러오기 다이얼로그 표시
- [ ] "계속하기" 클릭 시 금화·배·퀘스트 복원

- [ ] **Step 4: 커밋**

```bash
git add src/systems/save.js src/App.jsx
git commit -m "feat: add save/load system with localStorage pioneer_save_v2"
```

---

## Task 2: TutorialOverlay 컴포넌트

**Files:**
- Create: `src/components/TutorialOverlay.jsx`

- [ ] **Step 1: 파일 생성**

```jsx
// src/components/TutorialOverlay.jsx
import React, { useState } from 'react';

const WORLD_SLIDES = [
  {
    icon: '🌊',
    title: '대항해시대, 1492년',
    body: '당신은 빚더미 위의 신출내기 상인.\n단 하나의 낡은 상인선과 100금만으로\n세계 최고의 무역왕이 되어야 합니다.',
  },
  {
    icon: '🏛️',
    title: '왕국의 세금',
    body: '왕국은 당신의 성장을 탐냅니다.\n배가 늘수록, 자산이 쌓일수록\n세금은 끝없이 늘어납니다.\n\n살아남을 수 있겠습니까?',
  },
  {
    icon: '⏳',
    title: '방치의 전략',
    body: '배를 보내고 앱을 닫아도 됩니다.\n항해는 계속됩니다.\n1시간마다 시세가 바뀝니다.\n\n도착하면 돌아와 거래하고\n다시 떠나세요.',
  },
];

const STEPS = [
  { step: 1, icon: '⛵', title: '첫 배를 구매하세요', body: '함선 탭에서 슬루프를 구매해보세요.\n배가 있어야 무역을 시작할 수 있습니다.', highlight: 'ship-tab' },
  { step: 2, icon: '👤', title: '선원을 고용하세요', body: '선원 탭에서 선원 1명을 고용하세요.\n항해사는 속도를, 상인은 수익을 높입니다.', highlight: 'crew-tab' },
  { step: 3, icon: '🗺️', title: '항해를 시작하세요', body: '배를 클릭한 뒤 목적지 항구를 클릭하세요.\n앱을 닫아도 항해는 계속됩니다!', highlight: 'map' },
  { step: 4, icon: '📦', title: '자원을 거래하세요', body: '도착 후 화물 탭에서 자원을 구매하고\n비싼 항구로 이동해 판매하세요.', highlight: 'cargo-tab' },
];

export default function TutorialOverlay({ tutorialStep, onSlideEnd, onStepDone }) {
  const [slideIdx, setSlideIdx] = useState(0);

  // 세계관 슬라이드 단계 (tutorialStep === 0)
  if (tutorialStep === 0) {
    const slide = WORLD_SLIDES[slideIdx];
    return (
      <div className="fixed inset-0 z-50 flex items-center justify-center"
        style={{ background: 'radial-gradient(ellipse at center, #0a2540 0%, #060e1a 100%)' }}>
        <div className="panel-cr p-8 max-w-sm w-full mx-4 text-center animate-fadeInUp">
          <div className="text-6xl mb-4">{slide.icon}</div>
          <h2 className="font-black text-xl mb-3" style={{ color: '#f5c842' }}>
            {slide.title}
          </h2>
          <p className="text-sm text-gray-300 whitespace-pre-line leading-relaxed mb-6">
            {slide.body}
          </p>
          <div className="flex gap-2 justify-center mb-4">
            {WORLD_SLIDES.map((_, i) => (
              <div key={i} className="w-2 h-2 rounded-full"
                style={{ background: i === slideIdx ? '#f5c842' : '#334155' }} />
            ))}
          </div>
          <button className="btn-cr w-full"
            onClick={() => {
              if (slideIdx < WORLD_SLIDES.length - 1) {
                setSlideIdx(slideIdx + 1);
              } else {
                onSlideEnd(); // tutorialStep → 1
              }
            }}>
            {slideIdx < WORLD_SLIDES.length - 1 ? '다음 →' : '게임 시작!'}
          </button>
        </div>
      </div>
    );
  }

  // 인터랙티브 튜토리얼 단계 (tutorialStep 1~4)
  const current = STEPS.find(s => s.step === tutorialStep);
  if (!current || tutorialStep < 0) return null;

  return (
    <div className="fixed bottom-4 left-1/2 z-40 animate-fadeInUp"
      style={{ transform: 'translateX(-50%)', maxWidth: '360px', width: '90%' }}>
      <div className="panel-cr p-4 text-sm">
        <div className="flex items-start gap-3">
          <span className="text-2xl">{current.icon}</span>
          <div className="flex-1">
            <p className="font-black mb-1" style={{ color: '#f5c842' }}>{current.title}</p>
            <p className="text-gray-300 whitespace-pre-line text-xs">{current.body}</p>
          </div>
        </div>
        <div className="flex gap-1 mt-3">
          {STEPS.map((s) => (
            <div key={s.step} className="flex-1 h-1 rounded-full"
              style={{ background: s.step <= tutorialStep ? '#f5c842' : '#334155' }} />
          ))}
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: App.jsx에 TutorialOverlay 연결**

App.jsx 상단에 import:
```js
import TutorialOverlay from './components/TutorialOverlay.jsx';
```

App.jsx state 추가/수정:
```js
// tutorialStep: 0=슬라이드, 1~4=단계별 가이드, -1=완료
const [tutorialStep, setTutorialStep] = useState(() => {
  return hasSave() ? -1 : 0; // 저장 데이터 있으면 튜토리얼 스킵
});
```

App.jsx 배 구매 핸들러 완료 후:
```js
if (tutorialStep === 1) setTutorialStep(2);
```

App.jsx 선원 고용 핸들러 완료 후:
```js
if (tutorialStep === 2) setTutorialStep(3);
```

App.jsx 항해 출발 완료 후:
```js
if (tutorialStep === 3) setTutorialStep(4);
```

App.jsx 첫 구매 완료 후:
```js
if (tutorialStep === 4) {
  setTutorialStep(-1);
  // 튜토리얼 완료 보상
  setGs(prev => ({ ...prev, gold: prev.gold + 500, gems: prev.gems + 2 }));
  addLog('🎉 튜토리얼 완료! 500금 + 💎2개 획득!');
}
```

App.jsx JSX에 TutorialOverlay 추가:
```jsx
<TutorialOverlay
  tutorialStep={tutorialStep}
  onSlideEnd={() => setTutorialStep(1)}
  onStepDone={(step) => setTutorialStep(step + 1)}
/>
```

- [ ] **Step 3: 빌드 + 수동 확인**

```bash
npm run build && npm run preview
```

수동 확인:
- [ ] 첫 실행 시 세계관 슬라이드 3장 표시
- [ ] "게임 시작!" 클릭 후 Step 1 가이드 표시
- [ ] 배 구매 시 Step 2로 전진
- [ ] 선원 고용 시 Step 3으로 전진
- [ ] 항해 출발 시 Step 4로 전진
- [ ] 첫 구매 시 튜토리얼 완료 + 보상 로그
- [ ] 저장 후 재실행 시 튜토리얼 스킵

- [ ] **Step 4: 커밋**

```bash
git add src/components/TutorialOverlay.jsx src/App.jsx
git commit -m "feat: add FTUE tutorial with world slides and step-by-step guide"
```

---

## Task 3: 보석 소비 5종 구현

**Files:**
- Create: `src/components/GemShop.jsx`
- Modify: `src/App.jsx`

- [ ] **Step 1: GemShop.jsx 생성**

```jsx
// src/components/GemShop.jsx
import React from 'react';
import { calcTax } from '../systems/economy.js';
import { SHIP_TYPES } from '../constants/index.js';

export default function GemShop({ gs, selectedShip, onSpend, onClose }) {
  const ship = gs.ships.find(s => s.id === selectedShip);
  const taxAmount = calcTax(gs.taxLevel, gs.ships.length);

  const actions = [
    {
      id: 'instant',
      icon: '⚡',
      label: '즉시 도착',
      desc: ship?.isMoving ? `${SHIP_TYPES[ship.type]?.name ?? '배'} 즉시 도착` : '항해 중인 배 없음',
      cost: 1,
      disabled: !ship?.isMoving,
    },
    {
      id: 'tax_exempt',
      icon: '🛡️',
      label: '세금 면제',
      desc: `다음 세금 1회 면제 (${taxAmount.toLocaleString()}금)`,
      cost: 2,
      disabled: false,
    },
    {
      id: 'hire_expert',
      icon: '👤',
      label: '전문가 선원',
      desc: '전문가 등급 선원 즉시 고용',
      cost: 2,
      disabled: false,
    },
    {
      id: 'hire_legendary',
      icon: '🌟',
      label: '전설 선원',
      desc: '전설 등급 선원 즉시 고용',
      cost: 3,
      disabled: false,
    },
    {
      id: 'event_shield',
      icon: '🌀',
      label: '이벤트 방어막',
      desc: '다음 이벤트 1회 자동 회피',
      cost: 2,
      disabled: false,
    },
  ];

  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center"
      style={{ background: 'rgba(0,0,0,0.8)' }}>
      <div className="panel-cr p-6 max-w-sm w-full mx-4 animate-fadeInUp">
        <div className="flex justify-between items-center mb-4">
          <h2 className="font-black text-lg" style={{ color: '#00e5ff' }}>
            💎 보석 상점
          </h2>
          <span style={{ color: '#00e5ff', fontWeight: 700 }}>보유: {gs.gems}개</span>
        </div>

        <div className="flex flex-col gap-2">
          {actions.map(a => {
            const canAfford = gs.gems >= a.cost;
            const isDisabled = a.disabled || !canAfford;
            return (
              <div key={a.id} className="panel-cr p-3 flex items-center gap-3"
                style={{ opacity: isDisabled ? 0.5 : 1 }}>
                <span className="text-2xl">{a.icon}</span>
                <div className="flex-1">
                  <p className="font-bold text-sm" style={{ color: '#f5c842' }}>{a.label}</p>
                  <p className="text-xs text-gray-400">{a.desc}</p>
                </div>
                <button
                  className={isDisabled ? 'btn-cr opacity-50 cursor-not-allowed text-xs' : 'btn-cr text-xs'}
                  onClick={() => !isDisabled && onSpend(a.id, a.cost)}
                  disabled={isDisabled}
                >
                  💎 {a.cost}
                </button>
              </div>
            );
          })}
        </div>

        <button className="btn-cr-danger w-full mt-4 text-sm" onClick={onClose}>
          닫기
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: App.jsx에 GemShop 연결**

App.jsx 상단에 import:
```js
import GemShop from './components/GemShop.jsx';
```

App.jsx state 추가:
```js
const [showGemShop, setShowGemShop] = useState(false);
const [eventShieldActive, setEventShieldActive] = useState(false);
```

App.jsx 보석 소비 핸들러:
```js
const handleGemSpend = useCallback((actionId, cost) => {
  if (gsRef.current.gems < cost) return;

  setGs(prev => ({ ...prev, gems: prev.gems - cost }));

  switch (actionId) {
    case 'instant': {
      const ship = gsRef.current.ships.find(s => s.id === selShip);
      if (!ship?.isMoving) break;
      const pk = Object.entries(PORTS).find(([, p]) => p.x === ship.targetX && p.y === ship.targetY)?.[0];
      setGs(prev => ({
        ...prev,
        ships: prev.ships.map(s => s.id === selShip
          ? { ...s, x: ship.targetX, y: ship.targetY, isMoving: false, targetX: null, targetY: null, startX: null, startY: null }
          : s),
      }));
      addLog(`⚡ 보석으로 ${ship.name} 즉시 도착!`);
      break;
    }
    case 'tax_exempt':
      setNextTax(p => p + 1440); // 24분 연장
      addLog('🛡️ 다음 세금 1회 면제!');
      break;
    case 'hire_expert': {
      const expert = makeCrew('expert');
      setGs(prev => ({ ...prev, availableCrew: [expert, ...prev.availableCrew] }));
      addLog(`👤 전문가 선원 ${expert.name} 등장!`);
      break;
    }
    case 'hire_legendary': {
      const legend = makeCrew('legendary');
      setGs(prev => ({ ...prev, availableCrew: [legend, ...prev.availableCrew] }));
      addLog(`🌟 전설 선원 ${legend.name} 등장!`);
      break;
    }
    case 'event_shield':
      setEventShieldActive(true);
      addLog('🌀 이벤트 방어막 활성화! 다음 이벤트 1회 자동 회피');
      break;
  }
  setShowGemShop(false);
}, [selShip, addLog]);
```

App.jsx 이벤트 판정 부분에 방어막 확인 추가:
```js
// 이벤트 발생 전 방어막 체크
if (eventShieldActive && !evt.positive) {
  setEventShieldActive(false);
  addLog(`🌀 방어막으로 ${evt.label} 자동 회피!`);
  return; // 이벤트 무효
}
```

App.jsx 헤더에 보석 버튼 추가:
```jsx
<button className="btn-cr text-xs" onClick={() => setShowGemShop(true)}
  style={{ background: 'linear-gradient(180deg, #00e5ff 0%, #0099aa 100%)',
           border: '2px solid #007a8a', borderBottom: '4px solid #005a6a' }}>
  💎 {gs.gems} 사용
</button>
```

App.jsx JSX에 GemShop 추가:
```jsx
{showGemShop && (
  <GemShop
    gs={gs}
    selectedShip={selShip}
    onSpend={handleGemSpend}
    onClose={() => setShowGemShop(false)}
  />
)}
```

- [ ] **Step 3: 빌드 + 수동 확인**

```bash
npm run build && npm run preview
```

수동 확인:
- [ ] 헤더 보석 버튼 클릭 시 GemShop 팝업 표시
- [ ] 즉시 도착: 항해 중 배 있을 때 버튼 활성화, 클릭 시 즉시 도착
- [ ] 세금 면제: 클릭 시 nextTax 연장 로그
- [ ] 전문가 선원: 클릭 시 선원 목록에 전문가 등급 추가
- [ ] 이벤트 방어막: 활성화 후 이벤트 발생 시 자동 회피

- [ ] **Step 4: 커밋**

```bash
git add src/components/GemShop.jsx src/App.jsx
git commit -m "feat: add GemShop with 5 gem consumption types"
```

---

## Task 4: 선원 패널 CR 스타일 + 역할 표시

**Files:**
- Modify: `src/App.jsx` (선원 탭 렌더링)

- [ ] **Step 1: 선원 카드 등급 스타일 적용**

App.jsx 선원 카드 렌더링 부분에서 등급별 클래스 적용:

```jsx
{/* 선원 카드 — 변경 후 */}
{gs.availableCrew.map(crew => (
  <div key={crew.id}
    className={`rounded-xl p-3 text-xs animate-fadeInUp ${
      crew.grade === 'legendary' ? 'card-legendary' :
      crew.grade === 'expert'    ? 'card-expert'    :
      crew.grade === 'skilled'   ? 'card-skilled'   : 'card-common'
    }`}
    style={{ background: 'linear-gradient(180deg, #1e3a5f 0%, #152d4a 100%)' }}>
    <div className="flex justify-between items-start mb-1">
      <span className="font-bold" style={{ color: '#f5c842' }}>
        {crew.label ?? crew.name}
      </span>
      <span className="text-xs" style={{
        color: crew.grade === 'legendary' ? '#e67e22' :
               crew.grade === 'expert'    ? '#9b59b6' :
               crew.grade === 'skilled'   ? '#2980b9' : '#7f8c8d',
      }}>
        {crew.grade === 'legendary' ? '🌟전설' :
         crew.grade === 'expert'    ? '💜전문가' :
         crew.grade === 'skilled'   ? '💙숙련' : '⬜일반'}
      </span>
    </div>
    {/* 역할 표시 */}
    {crew.role && (
      <div className="mb-1" style={{ color: '#00e5ff', fontSize: '0.65rem' }}>
        {crew.role === 'navigator' ? '⚓ 항해사' :
         crew.role === 'merchant'  ? '💰 상인' :
         crew.role === 'cargo'     ? '📦 화물관리사' : '⚙️ 기관사'}
        {' '}+{Math.round(crew.primaryStat * 100)}%
      </div>
    )}
    <div className="flex justify-between items-center mt-2">
      <span className="text-gray-400">고용: {crew.hireCost.toLocaleString()}금</span>
      <button className="btn-cr text-xs py-0.5 px-2">고용</button>
    </div>
  </div>
))}
```

- [ ] **Step 2: 빌드 + 확인**

```bash
npm run build && npm run preview
```

수동 확인:
- [ ] 선원 등급별 테두리 색상 구분 (회색/파랑/보라/주황)
- [ ] 역할 + primaryStat 퍼센트 표시
- [ ] 전설 등급 주황 글로우 효과

- [ ] **Step 3: 커밋**

```bash
git add src/App.jsx
git commit -m "feat: apply grade-based card styles and role display to crew panel"
```

---

## Task 5: 최종 빌드 및 E2E 수동 검증

**Files:**
- None (검증만)

- [ ] **Step 1: 최종 빌드**

```bash
npm run build
```

Expected: 에러·경고 없음, `dist/` 생성

- [ ] **Step 2: 프리뷰 실행**

```bash
npm run preview
```

- [ ] **Step 3: E2E 수동 검증 체크리스트**

**비주얼**
- [ ] 지도 딥 네이비 그라데이션 배경
- [ ] 항구 지역별 이모지 + 골드 배지
- [ ] 배 아이콘 흰 글로우
- [ ] 항로 골드 점선 흐르는 애니메이션
- [ ] 헤더 골드 테두리 + 로고 글로우
- [ ] 버튼 3D 눌림 효과 (클릭 시 translateY)
- [ ] 선원 카드 등급별 테두리

**저장/불러오기**
- [ ] 저장 버튼 → "저장됨" 메시지
- [ ] 새로고침 → 불러오기 다이얼로그 → 금화·배·퀘스트 복원

**FTUE**
- [ ] 첫 실행 시 슬라이드 3장
- [ ] 배 구매·선원 고용·항해·거래 순서로 단계 진행
- [ ] 완료 시 500금 + 보석 2개 지급

**세금**
- [ ] 헤더 카운트다운 표시
- [ ] 24분 후 자동 징수 로그
- [ ] 배 구매 시 세금 레벨 +1

**이벤트**
- [ ] 항해 중 이벤트 팝업 등장
- [ ] 보석 회피 버튼 작동
- [ ] 이벤트 방어막 적용 시 자동 회피 로그

**퀘스트**
- [ ] 퀘스트 탭 표시
- [ ] 항구 도착 시 0~2개 생성
- [ ] 진행바 + 타이머 정상 표시
- [ ] 완료 시 "보상 수령" 버튼 → 금화 지급

**보석 소비**
- [ ] 5종 버튼 각각 정상 작동
- [ ] 보석 부족 시 버튼 비활성화

- [ ] **Step 4: 최종 커밋**

```bash
git add -A
git commit -m "feat: Pioneer v2.0 complete — save/load, FTUE, gem shop, event/quest/crew systems, CR visual"
```
