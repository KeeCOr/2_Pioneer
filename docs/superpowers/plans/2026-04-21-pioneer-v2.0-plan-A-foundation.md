# Pioneer v2.0 — Plan A: Foundation (파일 구조 + 비주얼)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** App.jsx 단일 파일을 constants/systems/components 구조로 분리하고, Clash Royale 스타일 비주얼을 적용한다.

**Architecture:** 기존 App.jsx에서 상수·시스템·컴포넌트를 추출해 각 파일로 이동. App.jsx는 import 조율자 역할만 유지. CSS/Tailwind를 CR 팔레트로 전면 교체.

**Tech Stack:** React 18, Vite 5, TailwindCSS 3

---

## 파일 맵

| 파일 | 역할 | 상태 |
|------|------|------|
| `src/constants/ships.js` | SHIP_TYPES, PORT_SHIPS | 신규 |
| `src/constants/ports.js` | PORTS, REGION_STYLE, RESOURCE_REGIONS | 신규 |
| `src/constants/resources.js` | RESOURCES, PORT_INFO, infoCurrentCost | 신규 |
| `src/constants/crew.js` | CREW_NAMES, SPECIAL_CREW_POOL, makeCrew, makePrediction | 신규 |
| `src/constants/index.js` | 전체 re-export | 신규 |
| `tailwind.config.js` | CR 컬러 팔레트 추가 | 수정 |
| `src/index.css` | CR 공통 클래스 추가 | 수정 |
| `src/App.jsx` | import 정리 (상수 참조 → constants/) | 수정 |

---

## Task 1: 디렉토리 생성

**Files:**
- Create: `src/constants/` (디렉토리)
- Create: `src/systems/` (디렉토리)
- Create: `src/components/` (디렉토리)

- [ ] **Step 1: 디렉토리 생성**

```bash
mkdir -p src/constants src/systems src/components
```

Expected: 3개 디렉토리 생성됨

- [ ] **Step 2: 확인**

```bash
ls src/
```

Expected: `App.jsx  constants/  components/  index.css  main.jsx  systems/`

---

## Task 2: ships.js 추출

**Files:**
- Create: `src/constants/ships.js`

- [ ] **Step 1: 파일 생성**

```js
// src/constants/ships.js
export const SHIP_TYPES = {
  rowboat:    { name: '통통배',   icon: '🚤',   desc: '초소형 쾌속선. 단거리 전용.',       baseSpeed: 0.014, baseCapacity: 25,  maxCrew: 2,  cost: 1000  },
  sloop:      { name: '슬루프',   icon: '⛵',    desc: '경쾌한 소형 쾌속선.',               baseSpeed: 0.010, baseCapacity: 55,  maxCrew: 4,  cost: 3000  },
  caravel:    { name: '카라벨',   icon: '🛥️',  desc: '탐험용 중형 쾌속선.',               baseSpeed: 0.009, baseCapacity: 80,  maxCrew: 6,  cost: 8000  },
  brigantine: { name: '브리간틴', icon: '⛴️',  desc: '쾌속 중형 상인선.',                 baseSpeed: 0.008, baseCapacity: 100, maxCrew: 7,  cost: 12000 },
  galley:     { name: '갤리',     icon: '🚣',   desc: '지중해 특화. 노+돛 복합.',           baseSpeed: 0.006, baseCapacity: 90,  maxCrew: 12, cost: 14000 },
  dhow:       { name: '다우',     icon: '🛶',   desc: '인도양·아라비아해 최적 범선.',       baseSpeed: 0.009, baseCapacity: 85,  maxCrew: 6,  cost: 16000 },
  merchant:   { name: '상인선',   icon: '🚢',   desc: '균형 잡힌 표준 대형 상인선.',       baseSpeed: 0.006, baseCapacity: 140, maxCrew: 9,  cost: 20000 },
  fluyt:      { name: '플루트',   icon: '🛳️',  desc: '화물 특화 네덜란드 대형선.',        baseSpeed: 0.004, baseCapacity: 200, maxCrew: 8,  cost: 28000 },
  junk:       { name: '정크선',   icon: '🏮',   desc: '동아시아 대형 화물선.',              baseSpeed: 0.005, baseCapacity: 220, maxCrew: 10, cost: 32000 },
  galleon:    { name: '갤리온',   icon: '⚓',   desc: '대양 항해 서양 대형 범선. 장거리.',  baseSpeed: 0.002, baseCapacity: 280, maxCrew: 12, cost: 45000 },
  frigate:    { name: '프리깃',   icon: '🏴‍☠️', desc: '최고급 전투 쾌속함.',             baseSpeed: 0.013, baseCapacity: 120, maxCrew: 12, cost: 65000 },
};

export const PORT_SHIPS = {
  london:    ['sloop','brigantine','merchant','galleon'],
  bristol:   ['rowboat','sloop'],
  lisbon:    ['sloop','caravel','merchant','galleon'],
  hamburg:   ['rowboat','sloop','brigantine'],
  antwerp:   ['sloop','brigantine','fluyt'],
  marseille: ['rowboat','sloop','galley'],
  genoa:     ['sloop','galley','merchant'],
  venice:    ['galley','merchant','fluyt'],
  tripoli:   ['galley','dhow'],
  istanbul:  ['galley','merchant','brigantine'],
  alexandria:['galley','dhow','merchant'],
  aden:      ['dhow','merchant'],
  dubai:     ['dhow','merchant','galleon'],
  mumbai:    ['dhow','merchant','galleon'],
  goa:       ['dhow','merchant'],
  calicut:   ['dhow','junk'],
  colombo:   ['dhow','merchant'],
  malacca:   ['dhow','junk','merchant'],
  singapore: ['junk','merchant','galleon'],
  bangkok:   ['junk','merchant'],
  guangzhou: ['junk','galleon','merchant'],
  shanghai:  ['junk','galleon','frigate'],
  yokohama:  ['junk','galleon','frigate'],
};
```

- [ ] **Step 2: 커밋**

```bash
git add src/constants/ships.js
git commit -m "feat: extract SHIP_TYPES and PORT_SHIPS to constants/ships.js"
```

---

## Task 3: ports.js 추출

**Files:**
- Create: `src/constants/ports.js`

- [ ] **Step 1: 파일 생성**

```js
// src/constants/ports.js
export const PORTS = {
  london:    { name: '런던',         region: 'europe',        country: '🇬🇧', x: 8,  y: 5  },
  bristol:   { name: '브리스톨',     region: 'europe',        country: '🇬🇧', x: 3,  y: 12 },
  lisbon:    { name: '리스본',       region: 'europe',        country: '🇵🇹', x: 3,  y: 24 },
  hamburg:   { name: '함부르크',     region: 'europe',        country: '🇩🇪', x: 18, y: 2  },
  antwerp:   { name: '앤트워프',     region: 'europe',        country: '🇧🇪', x: 21, y: 5  },
  marseille: { name: '마르세유',     region: 'mediterranean', country: '🇫🇷', x: 22, y: 30 },
  genoa:     { name: '제노바',       region: 'mediterranean', country: '🇮🇹', x: 26, y: 23 },
  venice:    { name: '베니스',       region: 'mediterranean', country: '🇮🇹', x: 36, y: 12 },
  tripoli:   { name: '트리폴리',     region: 'mediterranean', country: '🇱🇾', x: 38, y: 40 },
  istanbul:  { name: '이스탄불',     region: 'mediterranean', country: '🇹🇷', x: 47, y: 18 },
  alexandria:{ name: '알렉산드리아', region: 'arabian',       country: '🇪🇬', x: 43, y: 33 },
  aden:      { name: '아덴',         region: 'arabian',       country: '🇾🇪', x: 52, y: 45 },
  dubai:     { name: '두바이',       region: 'arabian',       country: '🇦🇪', x: 61, y: 33 },
  mumbai:    { name: '뭄바이',       region: 'south_asia',    country: '🇮🇳', x: 49, y: 53 },
  goa:       { name: '고아',         region: 'south_asia',    country: '🇮🇳', x: 52, y: 58 },
  calicut:   { name: '칼리컷',       region: 'south_asia',    country: '🇮🇳', x: 57, y: 65 },
  colombo:   { name: '콜롬보',       region: 'south_asia',    country: '🇱🇰', x: 66, y: 74 },
  malacca:   { name: '말라카',       region: 'east_asia',     country: '🇲🇾', x: 68, y: 59 },
  singapore: { name: '싱가포르',     region: 'east_asia',     country: '🇸🇬', x: 76, y: 63 },
  bangkok:   { name: '방콕',         region: 'east_asia',     country: '🇹🇭', x: 71, y: 50 },
  guangzhou: { name: '광저우',       region: 'east_asia',     country: '🇨🇳', x: 82, y: 36 },
  shanghai:  { name: '상하이',       region: 'east_asia',     country: '🇨🇳', x: 86, y: 26 },
  yokohama:  { name: '요코하마',     region: 'east_asia',     country: '🇯🇵', x: 94, y: 15 },
};

export const REGION_STYLE = {
  europe:       { icon: '🏰', color: '#60a5fa', border: 'border-blue-400',   label: '유럽'    },
  mediterranean:{ icon: '⛪', color: '#a78bfa', border: 'border-purple-400', label: '지중해'  },
  arabian:      { icon: '🕌', color: '#fb923c', border: 'border-orange-400', label: '아라비아' },
  south_asia:   { icon: '🛕', color: '#34d399', border: 'border-green-400',  label: '남아시아' },
  east_asia:    { icon: '🏯', color: '#f87171', border: 'border-red-400',    label: '동아시아' },
};

export const RESOURCE_REGIONS = {
  '양털':      { cheap: ['europe'],                    expensive: ['east_asia', 'arabian']               },
  '와인':      { cheap: ['europe', 'mediterranean'],   expensive: ['east_asia', 'south_asia']            },
  '다이아몬드':{ cheap: ['mediterranean'],             expensive: ['east_asia', 'south_asia']            },
  '향신료':    { cheap: ['south_asia', 'east_asia'],   expensive: ['europe', 'mediterranean']            },
  '도자기':    { cheap: ['east_asia'],                 expensive: ['europe', 'mediterranean']            },
  '비단':      { cheap: ['east_asia'],                 expensive: ['europe', 'arabian']                  },
  '해산물':    { cheap: ['mediterranean', 'europe'],   expensive: ['east_asia', 'arabian']               },
  '면직물':    { cheap: ['south_asia'],                expensive: ['europe', 'east_asia']                },
  '계피':      { cheap: ['south_asia'],                expensive: ['europe', 'arabian', 'mediterranean'] },
  '쌀':        { cheap: ['east_asia', 'south_asia'],   expensive: ['europe', 'arabian']                  },
};

export const portOf = (s) => {
  const e = Object.entries(PORTS).find(([, p]) => Math.abs(s.x - p.x) < 1.5 && Math.abs(s.y - p.y) < 1.5);
  return e ? e[0] : null;
};

export const routeRegionOf = (s) => {
  if (s.targetX !== null && s.targetY !== null) {
    const best = Object.entries(PORTS).reduce((b, [, p]) => {
      const d = Math.hypot(s.targetX - p.x, s.targetY - p.y);
      return d < b.d ? { d, region: p.region } : b;
    }, { d: Infinity, region: null });
    if (best.region) return best.region;
  }
  const pk = portOf(s);
  return pk ? PORTS[pk]?.region : null;
};
```

- [ ] **Step 2: 커밋**

```bash
git add src/constants/ports.js
git commit -m "feat: extract PORTS, REGION_STYLE, RESOURCE_REGIONS to constants/ports.js"
```

---

## Task 4: resources.js 추출

**Files:**
- Create: `src/constants/resources.js`

- [ ] **Step 1: 파일 생성**

```js
// src/constants/resources.js
export const RESOURCES = {
  '향신료':    { icon: '🌶️' },
  '도자기':    { icon: '🏺' },
  '비단':      { icon: '🧣' },
  '와인':      { icon: '🍷' },
  '다이아몬드':{ icon: '💎' },
  '해산물':    { icon: '🦐' },
  '면직물':    { icon: '📦' },
  '양털':      { icon: '🧶' },
  '계피':      { icon: '🌰' },
  '쌀':        { icon: '🍚' },
};

export const PORT_INFO = [
  { id: 'rumor',    tier: 'basic',   baseCost: 300,  name: '거리 소문',        desc: '귀동냥 시세 동향.',            accuracy: 0.30, magMin: 15,  magMax: 45,  repeat: true  },
  { id: 'hint',     tier: 'basic',   baseCost: 700,  name: '상인 귀띔',        desc: '상인에게 들은 시세 동향.',      accuracy: 0.40, magMin: 25,  magMax: 65,  repeat: true  },
  { id: 'analysis', tier: 'premium', baseCost: 3000, name: '상업 분석 보고서', desc: '전문 분석가 예측. (1회)',       accuracy: 0.58, magMin: 60,  magMax: 130, repeat: false },
  { id: 'route',    tier: 'premium', baseCost: 8000, name: '내부 정보',        desc: '항구 관리인 내부 정보. (1회)', accuracy: 0.72, magMin: 100, magMax: 200, repeat: false },
];

export const infoCurrentCost = (info, bc) =>
  Math.floor(info.baseCost * Math.pow(1.5, bc[info.id] || 0));
```

- [ ] **Step 2: 커밋**

```bash
git add src/constants/resources.js
git commit -m "feat: extract RESOURCES, PORT_INFO to constants/resources.js"
```

---

## Task 5: crew.js 추출

**Files:**
- Create: `src/constants/crew.js`

- [ ] **Step 1: 파일 생성**

```js
// src/constants/crew.js
import { PORTS } from './ports.js';
import { RESOURCES } from './resources.js';

export const CREW_ROLES = {
  navigator: { label: '항해사', icon: '⚓', primary: 'speed',    secondary: 'eventReduction' },
  merchant:  { label: '상인',   icon: '💰', primary: 'trade',    secondary: null             },
  cargo:     { label: '화물관리사', icon: '📦', primary: 'capacity', secondary: 'fuelReduction' },
  engineer:  { label: '기관사', icon: '⚙️', primary: 'fuel',    secondary: 'repairDiscount' },
};

export const SPECIAL_CREW_POOL = [
  { name: '이순신',       specialty: 'east_asia',     navBonus: 55, tradeBonus: 25, rarity: 'legendary', label: '🌟전설의 장군',     role: 'navigator' },
  { name: '정화 제독',    specialty: 'any',           navBonus: 45, tradeBonus: 45, rarity: 'legendary', label: '🌟대항해 제독',     role: 'navigator' },
  { name: '바스코',       specialty: 'south_asia',    navBonus: 50, tradeBonus: 20, rarity: 'rare',      label: '💜인도항로 개척자', role: 'navigator' },
  { name: '마르코',       specialty: 'any',           navBonus: 20, tradeBonus: 55, rarity: 'rare',      label: '💜실크로드 상인',   role: 'merchant'  },
  { name: '지중해 뱃사람',specialty: 'mediterranean', navBonus: 40, tradeBonus: 15, rarity: 'uncommon',  label: '💙지중해 전문가',   role: 'navigator' },
  { name: '아라비아 상인',specialty: 'arabian',       navBonus: 15, tradeBonus: 45, rarity: 'uncommon',  label: '💙아라비아 상인',   role: 'merchant'  },
  { name: '동아시아 항법',specialty: 'east_asia',     navBonus: 45, tradeBonus: 10, rarity: 'uncommon',  label: '💙동아시아 항법사', role: 'navigator' },
  { name: '인도 중개인',  specialty: 'south_asia',    navBonus: 10, tradeBonus: 45, rarity: 'uncommon',  label: '💙인도 중개인',     role: 'merchant'  },
  { name: '유럽 선장',    specialty: 'europe',        navBonus: 35, tradeBonus: 35, rarity: 'uncommon',  label: '💙유럽 선장',       role: 'navigator' },
];

export const CREW_NAMES = [
  '김해룡','이바람','박정현','최강석','정승호','장민우','오선장','신무적',
  '한파도','윤청해','임항해','서무역','조상인','강탐험','백용사','류대항',
  '문원양','권북해','노선비','채항도',
];

// 등급별 primaryStat 범위
const GRADE_STAT = {
  common:    [0.05, 0.10],
  skilled:   [0.10, 0.18],
  expert:    [0.18, 0.28],
  legendary: [0.28, 0.40],
};

const ROLES = Object.keys(CREW_ROLES);

let _crewSeed = 100;
export const makeCrew = (forcedGrade = null) => {
  const id = _crewSeed++;
  const roll = Math.random();
  let special = null;
  if (roll < 0.03)      special = SPECIAL_CREW_POOL.filter(c => c.rarity === 'legendary')[Math.floor(Math.random() * 2)];
  else if (roll < 0.12) special = SPECIAL_CREW_POOL.filter(c => c.rarity === 'rare')[Math.floor(Math.random() * 2)];
  else if (roll < 0.35) special = SPECIAL_CREW_POOL.filter(c => c.rarity === 'uncommon')[Math.floor(Math.random() * 5)];

  const grade = forcedGrade ?? (special
    ? (special.rarity === 'legendary' ? 'legendary' : special.rarity === 'rare' ? 'expert' : 'skilled')
    : 'common');
  const [minS, maxS] = GRADE_STAT[grade];
  const primaryStat = parseFloat((minS + Math.random() * (maxS - minS)).toFixed(3));
  const role = special?.role ?? ROLES[Math.floor(Math.random() * ROLES.length)];

  return {
    id,
    name:       special ? special.name : CREW_NAMES[id % CREW_NAMES.length],
    role,
    grade,
    primaryStat,
    navigation: Math.floor(30 + Math.random() * 70),
    trading:    Math.floor(30 + Math.random() * 70),
    stamina:    Math.floor(30 + Math.random() * 70),
    repair:     Math.floor(Math.random() * 60),
    hireCost:   special ? Math.floor(1500 + Math.random() * 5000) : Math.floor(500 + Math.random() * 1500),
    gemCost:    grade === 'expert' ? (1 + Math.floor(Math.random() * 2)) : grade === 'legendary' ? 3 : 0,
    shipId:     null,
    specialty:  special?.specialty || null,
    navBonus:   special?.navBonus  || 0,
    tradeBonus: special?.tradeBonus || 0,
    rarity:     special?.rarity || 'common',
    label:      special?.label || null,
  };
};

let _predId = 1;
export const makePrediction = (infoId, tier, portKey, portName, accuracy, magMin, magMax) => {
  const resources = Object.keys(RESOURCES);
  const portKeys  = Object.keys(PORTS);
  const resource  = resources[Math.floor(Math.random() * resources.length)];
  const targetPort= portKeys[Math.floor(Math.random() * portKeys.length)];
  const direction = Math.random() < 0.5 ? 'up' : 'down';
  const mag = Math.floor(magMin + Math.random() * (magMax - magMin));
  return { id: _predId++, infoId, tier, resource, targetPort,
    targetPortName: PORTS[targetPort].name, direction, accuracy, mag,
    applied: false, hit: null, boughtAt: portName };
};
```

- [ ] **Step 2: 커밋**

```bash
git add src/constants/crew.js
git commit -m "feat: extract crew constants with role system to constants/crew.js"
```

---

## Task 6: constants/index.js (re-export)

**Files:**
- Create: `src/constants/index.js`

- [ ] **Step 1: 파일 생성**

```js
// src/constants/index.js
export * from './ships.js';
export * from './ports.js';
export * from './resources.js';
export * from './crew.js';
```

- [ ] **Step 2: 커밋**

```bash
git add src/constants/index.js
git commit -m "feat: add constants/index.js re-export barrel"
```

---

## Task 7: App.jsx import 교체

**Files:**
- Modify: `src/App.jsx` (상단 상수 선언부 제거, import로 교체)

- [ ] **Step 1: App.jsx 상단에 import 추가 (기존 상수 선언 직전)**

App.jsx 파일 최상단 (`import React, ...` 바로 다음 줄)에 추가:

```js
import {
  SHIP_TYPES, PORT_SHIPS,
  PORTS, REGION_STYLE, RESOURCE_REGIONS, portOf, routeRegionOf,
  RESOURCES, PORT_INFO, infoCurrentCost,
  CREW_NAMES, SPECIAL_CREW_POOL, CREW_ROLES, makeCrew, makePrediction,
} from './constants/index.js';
```

- [ ] **Step 2: App.jsx에서 중복 선언 제거**

App.jsx 내 아래 상수 블록 전체 삭제 (이미 constants/에 있음):
- `const SHIP_TYPES = { ... }` (4~16줄)
- `const PORTS = { ... }` (18~42줄)
- `const RESOURCES = { ... }` (44~48줄)
- `const PORT_SHIPS = { ... }` (50~62줄)
- `const REGION_STYLE = { ... }` (64~70줄)
- `const RESOURCE_REGIONS = { ... }` (72~83줄)
- `const PORT_INFO = [ ... ]` (95~100줄)
- `const infoCurrentCost = ...` (101줄)
- `const SPECIAL_CREW_POOL = [ ... ]` (103~113줄)
- `const CREW_NAMES = [ ... ]` (178줄)
- `const makeCrew = () => { ... }` (180~198줄)
- `const makePrediction = ...` (166~176줄)
- `const portOf = ...` (121~124줄)
- `const routeRegionOf = ...` (125~135줄)

- [ ] **Step 3: 빌드 확인**

```bash
npm run build
```

Expected: `dist/` 생성, 에러 없음

- [ ] **Step 4: 커밋**

```bash
git add src/App.jsx
git commit -m "refactor: replace inline constants with imports from constants/"
```

---

## Task 8: tailwind.config.js — CR 팔레트

**Files:**
- Modify: `tailwind.config.js`

- [ ] **Step 1: 파일 전체 교체**

```js
// tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        'ocean-dark':  '#1a3a52',
        'ocean-blue':  '#2d5f8d',
        'ocean-light': '#4a90b8',
        'gold':        '#d4a574',
        'gold-light':  '#e8c9a0',
        cr: {
          'bg-deep':    '#0d1b2a',
          'bg-mid':     '#1a2f4a',
          'bg-panel':   '#1e3a5f',
          'gold':       '#f5c842',
          'gold-dark':  '#c49a1a',
          'gold-shadow':'#6b4f0a',
          'gem':        '#00e5ff',
          'danger':     '#e74c3c',
          'rare':       '#2980b9',
          'epic':       '#9b59b6',
          'legendary':  '#e67e22',
          'common':     '#7f8c8d',
        },
      },
      boxShadow: {
        'cr-btn':   '0 4px 0 #3a2800, 0 0 12px rgba(245,200,66,0.4)',
        'cr-panel': '0 0 20px rgba(245,200,66,0.15)',
        'cr-gem':   '0 0 16px rgba(0,229,255,0.5)',
        'cr-epic':  '0 0 8px rgba(155,89,182,0.6)',
        'cr-legend':'0 0 12px rgba(230,126,34,0.8)',
      },
      backgroundImage: {
        'cr-ocean': 'radial-gradient(ellipse at 30% 40%, #0a3d5c 0%, #0d2137 60%, #061220 100%)',
        'cr-panel': 'linear-gradient(180deg, #1e3a5f 0%, #152d4a 100%)',
        'cr-btn':   'linear-gradient(180deg, #f5c842 0%, #e5a820 60%, #c49a1a 100%)',
        'cr-btn-red':'linear-gradient(180deg, #e74c3c 0%, #c0392b 100%)',
      },
    },
  },
  plugins: [],
};
```

- [ ] **Step 2: 빌드 확인**

```bash
npm run build
```

Expected: 에러 없음

- [ ] **Step 3: 커밋**

```bash
git add tailwind.config.js
git commit -m "feat: add Clash Royale color palette to tailwind config"
```

---

## Task 9: index.css — CR 공통 클래스

**Files:**
- Modify: `src/index.css`

- [ ] **Step 1: 파일 전체 교체**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

* { margin: 0; padding: 0; box-sizing: border-box; }

body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background: radial-gradient(ellipse at 30% 40%, #0a3d5c 0%, #0d2137 60%, #061220 100%);
  color: #e4d5c9;
  min-height: 100vh;
}

/* ── CR 버튼 ── */
.btn-cr {
  background: linear-gradient(180deg, #f5c842 0%, #e5a820 60%, #c49a1a 100%);
  border: 3px solid #8b6914;
  border-bottom: 5px solid #6b4f0a;
  border-radius: 12px;
  box-shadow: 0 4px 0 #3a2800, 0 0 12px rgba(245,200,66,0.4);
  color: #1a0a00;
  font-weight: 800;
  text-shadow: 0 1px 2px rgba(255,255,255,0.3);
  transition: transform 0.05s ease, border-bottom-width 0.05s ease;
  cursor: pointer;
  padding: 6px 14px;
}
.btn-cr:active {
  transform: translateY(3px);
  border-bottom-width: 2px;
}
.btn-cr:disabled {
  filter: grayscale(0.6);
  opacity: 0.6;
  cursor: not-allowed;
}

/* ── CR 위험 버튼 ── */
.btn-cr-danger {
  background: linear-gradient(180deg, #e74c3c 0%, #c0392b 100%);
  border: 3px solid #922b21;
  border-bottom: 5px solid #641e16;
  border-radius: 12px;
  box-shadow: 0 4px 0 #3b0f0a;
  color: #fff;
  font-weight: 800;
  text-shadow: 0 1px 2px rgba(0,0,0,0.5);
  transition: transform 0.05s ease, border-bottom-width 0.05s ease;
  cursor: pointer;
  padding: 6px 14px;
}
.btn-cr-danger:active { transform: translateY(3px); border-bottom-width: 2px; }

/* ── CR 패널 ── */
.panel-cr {
  background: linear-gradient(180deg, #1e3a5f 0%, #152d4a 100%);
  border: 2px solid #f5c842;
  border-radius: 16px;
  box-shadow: inset 0 1px 0 rgba(255,255,255,0.1), 0 0 20px rgba(245,200,66,0.15);
}

/* ── 선원 카드 등급 테두리 ── */
.card-common    { border: 2px solid #7f8c8d; }
.card-skilled   { border: 2px solid #2980b9; box-shadow: 0 0 8px rgba(41,128,185,0.5); }
.card-expert    { border: 2px solid #9b59b6; box-shadow: 0 0 8px rgba(155,89,182,0.6); }
.card-legendary { border: 2px solid #e67e22; box-shadow: 0 0 12px rgba(230,126,34,0.8); }

/* ── 골드 펄스 (선택 항구) ── */
@keyframes cr-pulse {
  0%, 100% { box-shadow: 0 0 0 0 rgba(245,200,66,0.7); }
  50%       { box-shadow: 0 0 0 8px rgba(245,200,66,0); }
}
.pulse-gold { animation: cr-pulse 1.5s ease-in-out infinite; }

/* ── 항로 골드 점선 애니메이션 ── */
@keyframes dash-move { to { stroke-dashoffset: -20; } }
.route-line { animation: dash-move 0.8s linear infinite; }

/* ── 페이드인 ── */
@keyframes fadeInUp {
  from { opacity: 0; transform: translateY(10px); }
  to   { opacity: 1; transform: translateY(0); }
}
.animate-fadeInUp { animation: fadeInUp 0.4s ease-out; }

/* ── 흔들기 ── */
@keyframes shake {
  0%, 100% { transform: translateX(0); }
  25%       { transform: translateX(-3px); }
  75%       { transform: translateX(3px); }
}
.animate-shake { animation: shake 0.4s; }

/* ── 기존 호환 클래스 ── */
.card { @apply panel-cr p-4; }
.button-gold { @apply btn-cr; }
```

- [ ] **Step 2: 빌드 + 프리뷰 확인**

```bash
npm run build && npm run preview
```

Expected: 배경이 딥 네이비 그라데이션으로 변경됨

- [ ] **Step 3: 커밋**

```bash
git add src/index.css
git commit -m "feat: apply Clash Royale visual style to index.css"
```

---

## Task 10: 지도 비주얼 업데이트 (App.jsx SVG/지도 섹션)

**Files:**
- Modify: `src/App.jsx` (지도 렌더 부분)

- [ ] **Step 1: 지도 컨테이너 배경 교체**

App.jsx에서 지도 컨테이너 `div`를 찾아 배경색 변경:

```jsx
{/* 변경 전 */}
<div ref={mapRef} className="relative w-full h-full bg-blue-900 overflow-hidden ...">

{/* 변경 후 */}
<div ref={mapRef} className="relative w-full h-full overflow-hidden ..."
  style={{ background: 'radial-gradient(ellipse at 30% 40%, #0a3d5c 0%, #0d2137 60%, #061220 100%)' }}>
```

- [ ] **Step 2: 항구 마커 업데이트**

App.jsx에서 항구 마커 렌더링 부분을 찾아 교체:

```jsx
{/* 항구 마커 — 변경 후 */}
{Object.entries(PORTS).map(([pk, p]) => {
  const rs = REGION_STYLE[p.region];
  const isSelected = routeMode && selShip;
  return (
    <g key={pk}>
      {/* 선택 펄스 링 */}
      {isSelected && (
        <circle
          cx={`${p.x}%`} cy={`${p.y}%`}
          r="2.2%" fill="none"
          stroke="#f5c842" strokeWidth="0.3%"
          className="pulse-gold" style={{ transformOrigin: `${p.x}% ${p.y}%` }}
        />
      )}
      {/* 아이콘 */}
      <text
        x={`${p.x}%`} y={`${p.y}%`}
        textAnchor="middle" dominantBaseline="middle"
        fontSize="1.6%" style={{ filter: 'drop-shadow(0 0 3px rgba(0,0,0,0.8))' }}
      >
        {rs.icon}
      </text>
      {/* 이름 배지 */}
      <foreignObject x={`${p.x - 4}%`} y={`${p.y + 1.5}%`} width="8%" height="3%">
        <div style={{
          background: 'rgba(13,27,42,0.85)',
          border: '1px solid #f5c842',
          borderRadius: '4px',
          color: '#f5c842',
          fontSize: '0.55rem',
          textAlign: 'center',
          padding: '1px 3px',
          whiteSpace: 'nowrap',
        }}>
          {p.name}
        </div>
      </foreignObject>
    </g>
  );
})}
```

- [ ] **Step 3: 배 아이콘 글로우 추가**

App.jsx에서 배 렌더링 부분 찾아 `filter` 스타일 추가:

```jsx
{/* 배 아이콘 — 글로우 추가 */}
<text
  x={`${s.x}%`} y={`${s.y}%`}
  textAnchor="middle" dominantBaseline="middle"
  fontSize="2%"
  style={{ filter: 'drop-shadow(0 0 4px rgba(255,255,255,0.7)) drop-shadow(0 0 8px rgba(245,200,66,0.4))' }}
>
  {SHIP_TYPES[s.type].icon}
</text>
```

- [ ] **Step 4: 항로 선 골드로 변경**

App.jsx에서 항로 SVG line/polyline 찾아 stroke 색상 변경:

```jsx
{/* 항로 선 — 변경 후 */}
<line
  x1={`${s.startX}%`} y1={`${s.startY}%`}
  x2={`${s.targetX}%`} y2={`${s.targetY}%`}
  stroke="#f5c842" strokeWidth="0.3%"
  strokeDasharray="1.5% 1%"
  className="route-line"
/>
```

- [ ] **Step 5: 빌드 + 시각 확인**

```bash
npm run build && npm run preview
```

수동 확인:
- [ ] 지도 배경이 딥 네이비 그라데이션
- [ ] 항구가 지역별 이모지로 표시
- [ ] 항구 이름이 골드 배지
- [ ] 배 아이콘에 글로우 효과
- [ ] 항로가 골드 점선 + 흐르는 애니메이션

- [ ] **Step 6: 커밋**

```bash
git add src/App.jsx
git commit -m "feat: apply CR visual style to game map (gold routes, port badges, ship glow)"
```

---

## Task 11: 헤더 CR 스타일 적용

**Files:**
- Modify: `src/App.jsx` (헤더 섹션)

- [ ] **Step 1: 헤더 배경·테두리 교체**

App.jsx 헤더 JSX를 찾아:

```jsx
{/* 헤더 — 변경 후 */}
<header className="flex items-center justify-between px-4 py-2 text-sm"
  style={{
    background: '#0d1b2a',
    borderBottom: '2px solid #f5c842',
    boxShadow: '0 2px 12px rgba(245,200,66,0.2)',
  }}>
  {/* 로고 */}
  <span className="font-black text-lg tracking-widest"
    style={{ color: '#f5c842', textShadow: '0 0 10px rgba(245,200,66,0.5)' }}>
    ⚓ PIONEER
  </span>
  {/* 재화 */}
  <div className="flex gap-4 items-center">
    <span style={{ color: '#f5c842', fontWeight: 700 }}>
      💰 {gs.gold.toLocaleString()}금
    </span>
    <span style={{ color: '#00e5ff', fontWeight: 700 }}>
      💎 {gs.gems}
    </span>
  </div>
</header>
```

- [ ] **Step 2: 빌드 + 확인**

```bash
npm run build && npm run preview
```

수동 확인:
- [ ] 헤더 딥 네이비 배경 + 골드 하단 테두리
- [ ] 로고 골드 글로우
- [ ] 금화 골드, 보석 청록

- [ ] **Step 3: 커밋**

```bash
git add src/App.jsx
git commit -m "feat: apply CR style to game header"
```
